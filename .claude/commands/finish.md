Update CLAUDE.md to reflect any changes made in this session that
aren't already documented. Then commit all staged and unstaged
changes with a descriptive commit message summarising what was built
or fixed, and push to origin main.

After updating CLAUDE.md, check its size: count the characters in
the file. If it exceeds 35,000 characters, warn the user with a
message like: "⚠ CLAUDE.md is now N chars — over the 35k target.
Consider trimming: remove CSS class specifics, collapse
implementation walkthroughs into single-line references, or cut
sections whose details are readable from the source code."
Do not trim automatically — just warn.
